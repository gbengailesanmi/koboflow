{
  "name": "money-mapper-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @money-mapper/shared build && yarn workspace @money-mapper/web build",
    "build:shared": "yarn workspace @money-mapper/shared build",
    "build:web": "yarn build:shared && yarn workspace @money-mapper/web build",
    "dev:backend": "yarn workspace @money-mapper/backend dev",
    "postinstall": "yarn workspace @money-mapper/shared build && yarn workspace @money-mapper/web build",
    "dev:mobile": "yarn workspace @money-mapper/mobile start",
    "build:backend": "yarn workspace @money-mapper/backend build",
    "lint": "yarn workspaces run lint",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/.next packages/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "resolutions": {
    "next": "^16.1.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
